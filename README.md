	usage: ui.py [-h] [-r] [filename]

	Hex editor

	positional arguments:
	filename         name of the file to edit

	optional arguments:
		-h, --help       show this help message and exit
		-r, --read-only  opens file in readonly mode if passed